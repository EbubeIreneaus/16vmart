<script setup lang="ts">definePageMeta({ layout: 'admin' }); const activeRole = useMockRole(); const response = { items: [{ id: 1, fullname: 'Ada Nwosu', email: 'ada@example.com', role: 'seller', status: 'active', email_verified: true }], total: 1, page: 1, size: 50, pages: 1 }</script>
<template><div><p class="text-sm font-bold uppercase tracking-widest text-teal-700">Platform oversight</p><h1 class="mt-2 text-4xl font-black">Users</h1><div class="mt-7 rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><div v-for="user in response.items" :key="user.id" class="flex items-center justify-between"><div><p class="font-black">{{ user.fullname }}</p><p class="text-sm text-slate-500">{{ user.email }}</p></div><div class="flex gap-2"><span class="rounded-full bg-slate-100 px-3 py-1 text-sm font-bold capitalize">{{ user.role }}</span><button v-if="activeRole === 'superadmin'" class="rounded-lg border border-slate-300 px-3 py-2 text-sm font-bold">Change role</button></div></div></div></div></template>
